spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dpdb
    username: root
    password: lion
  jpa:
    generate-ddl: true
    show-sql: true
employee:
    defaultCategory: getAllEmployees
eureka:
  client:
    register-with-eureka: true
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka